import XCTest
import HTTP
@testable import NodesSSO

class NodesSSOTests: XCTestCase {
    func testExample() throws {
        XCTAssertTrue(true)
    }

    static var allTests : [(String, (NodesSSOTests) -> () throws -> Void)] {
        return [
            ("testExample", testExample),
        ]
    }
}
